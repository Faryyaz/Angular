import { AlternateHighlightDirective } from './alternate-highlight.directive';

describe('AlternateHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AlternateHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
